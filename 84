#include <stdio.h>

int main()
{
  long long int h, b, c, s;
  double result=0;
  scanf("%lld %lld %lld %lld", &h, &b, &c, &s);
  result = h*b*c*s;
  printf("%.1lf MiB", result/(8*1024*1024));
  return 0;
}
