#include <stdio.h>

int main()
{
   int n, m ,i, j, a=0, b=0;
   scanf("%d %d", &n, &m);
   a=a+n;
   b=b+m;
   int cnt=a*b;
   for(i=0; i<n; i++)
   {
       for(j=0; j<m; j++)
       {
           printf("%d ", cnt);
           cnt--;
       }
       printf("\n");
   }
}
