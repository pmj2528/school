#include <stdio.h>

int main()
{
  long long int a, b, c, d;
  scanf("%lld %lld %lld %lld", &a, &b, &c, &d);
lable:
  if(d!=1)
  {
    a=(a*b)+c;
    d--;
    goto lable;
  }
  else
  {
    printf("%lld", a);
  }
}
